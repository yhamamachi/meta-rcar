From 1e4782c28bc6a80b1666ba03f301169194170343 Mon Sep 17 00:00:00 2001
From: Vladimir Barinov <vladimir.barinov@cogentembedded.com>
Date: Thu, 21 May 2020 13:50:54 +0300
Subject: [PATCH 22/33] phy: rcar-gen3-usb2: power-on port in host mode to

Signed-off-by: Andrey Gusakov <andrey.gusakov@cogentembedded.com>
---
 drivers/phy/renesas/phy-rcar-gen3-usb2.c | 14 ++++++++++----
 1 file changed, 10 insertions(+), 4 deletions(-)

diff --git a/drivers/phy/renesas/phy-rcar-gen3-usb2.c b/drivers/phy/renesas/phy-rcar-gen3-usb2.c
index 898331f..d57f6fc 100644
--- a/drivers/phy/renesas/phy-rcar-gen3-usb2.c
+++ b/drivers/phy/renesas/phy-rcar-gen3-usb2.c
@@ -199,7 +199,8 @@ static void rcar_gen3_init_for_host(struct rcar_gen3_chan *ch)
 	rcar_gen3_enable_vbus_ctrl(ch, 1);
 
 	ch->extcon_host = true;
-	schedule_work(&ch->work);
+	if (ch->extcon)
+		schedule_work(&ch->work);
 }
 
 static void rcar_gen3_init_for_peri(struct rcar_gen3_chan *ch)
@@ -209,7 +210,8 @@ static void rcar_gen3_init_for_peri(struct rcar_gen3_chan *ch)
 	rcar_gen3_enable_vbus_ctrl(ch, 0);
 
 	ch->extcon_host = false;
-	schedule_work(&ch->work);
+	if (ch->extcon)
+		schedule_work(&ch->work);
 }
 
 static void rcar_gen3_init_for_b_host(struct rcar_gen3_chan *ch)
@@ -376,7 +378,7 @@ static void rcar_gen3_init_otg(struct rcar_gen3_chan *ch)
 
 	val = readl(usb2_base + USB2_VBCTRL);
 	val &= ~USB2_VBCTRL_OCCLREN;
-	writel(val | USB2_VBCTRL_DRVVBUSSEL, usb2_base + USB2_VBCTRL);
+	writel(val /*| USB2_VBCTRL_DRVVBUSSEL*/, usb2_base + USB2_VBCTRL);
 	val = readl(usb2_base + USB2_ADPCTRL);
 	writel(val | USB2_ADPCTRL_IDPULLUP, usb2_base + USB2_ADPCTRL);
 
@@ -401,13 +403,17 @@ static int rcar_gen3_phy_usb2_init(struct phy *p)
 	writel(val, usb2_base + USB2_INT_ENABLE);
 	writel(USB2_SPD_RSM_TIMSET_INIT, usb2_base + USB2_SPD_RSM_TIMSET);
 	writel(USB2_OC_TIMSET_INIT, usb2_base + USB2_OC_TIMSET);
+	val = readl(usb2_base + USB2_VBCTRL);
+	val |= USB2_VBCTRL_DRVVBUSSEL;
+	writel(val, usb2_base + USB2_VBCTRL);
 
 	/* Initialize otg part */
 	if (channel->is_otg_channel) {
 		if (rcar_gen3_needs_init_otg(channel))
 			rcar_gen3_init_otg(channel);
 		rphy->otg_initialized = true;
-	}
+	} else
+		rcar_gen3_init_for_host(channel);
 
 	rphy->initialized = true;
 
-- 
2.7.4

